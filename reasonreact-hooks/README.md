# ReasonReact Hooks Demo

Ensure parcel and requirements are installed:

```
type parcel || sudo npm install -g parcel
npm install
```

Then build and serve the application:

```
npm start & firefox localhost:1234 & npm run serve
```
